<p>
  <strong><%= t('.title')	  %></strong>
  <%= @category.name %>
</p>

<%= link_to t('common.update'), edit_admin_category_path(@category) %> |
<%= link_to t('common.back'), admin_categories_path %>
